# Jeff GitHub Pages

Content for the public site lives here. To preview locally:

```bash
cd doc/ghpages
bundle install --path ../../vendor/bundle
BUNDLE_PATH=../../vendor/bundle bundle exec jekyll serve --livereload --baseurl ""
```

The generated site will be available at [http://localhost:4000](http://localhost:4000).
