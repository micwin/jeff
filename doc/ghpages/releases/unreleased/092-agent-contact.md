- feat: add `jeff agent instructions` and `jeff agent contact` so specialist
  agents can discover the Jeff coordination protocol and contact the narrow
  coordinator session with an automatic gatehouse handoff fallback.
