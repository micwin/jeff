- feat: add `jeff specialists` (`jeff sp`) to list, inspect, and call registered specialists through a generic Jeff bridge with `codex-ctl` and deterministic echo transports.

- test: cover specialist registry listing, JSON inspection, completion, disabled and malformed entries, file-backed requests, transport failure handling, prompt wrapping, alias export, and call logging in Smokey.
